import 'package:flutter/material.dart';

class FinanceHomeScreen extends StatelessWidget {
  const FinanceHomeScreen({super.key});
  @override
  Widget build(BuildContext context) => const Center(child: Text('Finance Home'));
}

class FinanceApprovalsScreen extends StatelessWidget {
  const FinanceApprovalsScreen({super.key});
  @override
  Widget build(BuildContext context) => const Center(child: Text('Finance Approvals'));
}

class FinanceCardsScreen extends StatelessWidget {
  const FinanceCardsScreen({super.key});
  @override
  Widget build(BuildContext context) => const Center(child: Text('Finance Cards'));
}

class FinanceExpensesScreen extends StatelessWidget {
  const FinanceExpensesScreen({super.key});
  @override
  Widget build(BuildContext context) => const Center(child: Text('Finance Expenses'));
}

class FinanceAIScreen extends StatelessWidget {
  const FinanceAIScreen({super.key});
  @override
  Widget build(BuildContext context) => const Center(child: Text('Finance AI Insights'));
}
