import 'package:flutter/material.dart';

class LignColors {
  // Primary Accent (Electric Lime)
  static const Color electricLime = Color(0xFFB3FF41);

  // Backgrounds
  static const Color primaryBackground = Color(0xFFFFFFFF);
  static const Color secondaryBackground = Color(0xFFF7F7F7);

  // Text Colors
  static const Color textPrimary = Color(0xFF111111);
  static const Color textSecondary = Color(0xFF6B7280);
  static const Color textDisabled = Color(0xFF9CA3AF);

  // Borders
  static const Color border = Color(0xFFE5E5E5);

  // Status Colors
  static const Color success = electricLime; // Reusing lime for success as per brief
  static const Color warning = Color(0xFFFFA500); // Amber
  static const Color error = Color(0xFFFF4141); // Reddish

  // Card
  static const Color cardSurface = Color(0xFFFFFFFF);
}
